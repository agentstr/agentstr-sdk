name: custom_framework_agent  # Deployment name

file_path: getting_started/custom_framework_agent/main.py  # Path to main.py file

database: true  # Provision postgres database (if not already provisioned)

extra_pip_deps:  # Additional Python deps installed in image
  - agentstr-sdk==0.5.0

env:  # Environment Variables
  NOSTR_RELAYS: wss://relay.primal.net,wss://relay.damus.io,wss://nostr.mom

env_file: getting_started/custom_framework_agent/.env  # Path to .env file
